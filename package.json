{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../part2_notes_frontend && npm run build && cp -r dist ../part3_notes_backend",
    "deploy:full": "npm run build:ui && cd ../part3_notes_backend && git add . && git commit -m uibuild && git push",
    "test": "echo \\\"Error: no test specified\\\" && exit 1"
  },
  "author": "Arnav Goel",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
